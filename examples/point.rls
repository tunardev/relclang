struct Point {
    x: Int
    y: Int
}

fn dist2(a: Point, b: Point) -> Int {
    let dx = a.x - b.x
    let dy = a.y - b.y
    dx * dx + dy * dy
}

fn main() {
    let a = Point { x: 10, y: 20 }
    let b = Point { x: 15, y: 25 }
    println(dist2(a, b))
}
